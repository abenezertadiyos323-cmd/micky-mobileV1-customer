# Required: Convex deployment URL (baked in at build time by Vite)
# Copy this file to .env.local for local dev and set the DEV deployment URL.
# Set the production value in Vercel → Project Settings → Environment Variables.
VITE_CONVEX_URL=https://<your-deployment>.convex.cloud
