{
  "version": 2,
  "installCommand": "cd customer-app && corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "cd customer-app && corepack enable && pnpm run build",
  "outputDirectory": "customer-app/dist",
  "framework": "vite"
}
